When the Wizard generates the FoxPro Proxy class it will create type fixups for a few different types that are returned as result values from the proxy method calls.

<%=  ChildTopicsList(oHelp,"") %>